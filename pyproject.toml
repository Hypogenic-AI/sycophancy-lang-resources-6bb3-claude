[project]
name = "research-workspace"
version = "0.1.0"
description = "Research workspace for sycophancy and language resource experiments"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
